# Tasks

Derived from `docs/PLAN.md`. Format: `id — title — paths — deps — DoD`.
Agent column marks who executes after the approval gate: **parent** (shared/integration) or a named implementer subagent.

## M15 — Operations: the site survives being left alone *(serial, parent)*

> Iteration I3, from `docs/ROADMAP.md` R-01, R-02 and R-03, plus one defect the baseline found.
> Intake, impact map and exit criteria: `docs/iterations/I3-operations.md`. Nothing here is
> visible to a visitor. **R-01 is taken in its smallest honest form and is not closed by this
> milestone** — while the site is in test mode on the NAS the appliance's own snapshots are the
> backup (ADR-023) and the engineered form waits for the move to a dedicated server (ADR-024).
> **Three of these six tasks change shell scripts, deployment configuration and documentation
> that no test in this repository executes**, so their DoD says "run it" and means it — this
> project has already ticked one such task (T073) with half its DoD unmet and paid for it in T086.
> Further deferrals: ADR-025 (no notifier; the Telegram bot moves with ADR-024's trigger),
> ADR-026 (R-15).
>
> **Status 2026-08-15: the code is finished and this milestone is not.** T125, T126 and T130 are
> done — gates at the close were unit/API 277 exit 0, e2e 81 exit 0 twice, lint and format clean
> over 53 files. T127, T128 and T129 are written, committed and **deliberately left unticked**:
> each has a half that cannot be run from a session — a push to GitHub for T127, the TrueNAS
> interface for T128's snapshot task and T129's cron job. The remaining steps are listed under
> "Resume here" in `docs/STATUS.md`. Do not tick them until they have been run.

- [x] **T125** — a photograph whose stored files went is re-rendered, not a 500 — paths: `app/routers/photos.py`, `tests/api/test_photo.py` — deps: none — DoD: the baseline's intermittent e2e error is gone; `photo_upload`'s known-asset branch calls `images.stored_from_asset` **once** and treats an empty `derivatives` the way `missing_rungs` already treats an incomplete ladder — status `PENDING` and into the background pool — instead of falling into the branch that assumes the files are there; `_assign_renditions` never indexes an empty list, and the invariant its docstring already claims ("every case still has to leave a servable thumbnail") is the one it enforces; a test patches `images.renditions_of` to answer non-empty once and empty afterwards, reproducing the window between `find_asset` and `stored_from_asset`, and asserts **201 with `pending`**, not 500 — watched failing first; `tests/api/test_blog.py::test_a_second_upload_of_known_bytes_writes_no_second_original` and both `missing_rungs` tests pass unchanged; **F42 is not weakened — no second original is written in the recovery path either.**
- [x] **T126** — the log sticks out of the container, and no log can fill the disk — paths: `app/config.py`, `app/main.py`, `deploy/portainer-stack.yml`, `docker-compose.prod.yml`, `.env.example`, `tests/unit/test_logging.py` — deps: none — DoD: F58 and F60 met, per ADR-025; a new `LOG_DIR` setting, **empty by default**, so development and the test suite are untouched and nothing new is written on a developer's disk; when it is set, `app/main.py` adds a `RotatingFileHandler` on `<LOG_DIR>/app.log` beside the existing `basicConfig` stream — same format, same lines, stdout is **not** replaced — with `maxBytes` and `backupCount` chosen and justified in a comment; if the directory is missing or not writable the application **logs a warning and starts anyway on stdout only**, because a log path must never be the reason the site is down, and a unit test asserts exactly that; both deployment files bind-mount a host directory to it (`LOGS_HOST_DIR`, its own dataset, chowned to uid 1000 like the media one) and carry a `logging:` block with explicit `max-size` and `max-file` on **every** service, `docker compose config` rendering both to prove it; `.env.example` documents both variables; **no secret, session token, password or connection string reaches the file** — the review greps a produced log for the values in `.env`; the development `docker-compose.yml` is deliberately not changed.
- [x] **T127** — the suite gates the image — paths: `.github/workflows/publish.yml` — deps: none — DoD: F59 met; a `tests` job runs `docker compose run --rm tests` plus `ruff check` and `ruff format --check`, and `publish` declares `needs: tests`, so a red suite publishes nothing and moves no tag; the job writes its own `.env` with generated values rather than copying `.env.example`, whose `ADMIN_PASSWORD=change-me` the application refuses at startup by design — **that placeholder is not weakened to make CI easier**; the exit-code discipline from `CLAUDE.md` holds, no run is piped through `tail` or `grep`; e2e stays out of the gate and runs on `v*` tags only, where the extra minutes are affordable; the header comment that currently says "This workflow does not run the test suite" is rewritten rather than left contradicting the file; **verified in both directions on a scratch branch** — a deliberately red commit observed not publishing, then a green one observed publishing.
- [ ] **T128** — a backup can be taken where the site actually runs — paths: `scripts/backup.sh`, `docs/HANDOFF.md` §5 — deps: none — DoD: F57 met in the form ADR-023 gives it, and **R-01 is not claimed closed** (ADR-024); the script gains one additive branch — when `BACKUP_DB_CONTAINER` is set it reaches the database with `docker exec` on that container instead of `docker compose exec -T db`, everything else identical — so a run from a host checkout behaves exactly as it does today, proved by running it that way first and diffing the artefact names against a pre-change run; the flat 30-day prune, the retention policy, the manifest and the rehearsal harness are **not** built; §5 gains the TrueNAS Periodic Snapshot Task — which datasets, how often, how many kept, and the fact that a snapshot of a running Postgres is crash-consistent and not a substitute for the dump — plus the exact one-line command to take a dump on the server, and a statement that the three secrets live only in Portainer and are regenerated rather than restored; the snapshot task is **created on the appliance** and the dump command **run once there**, both recorded in `docs/iterations/I3-operations.md` with their real output; the existing restore procedure and the T086 rehearsal record are preserved, not rewritten.
- [x] **T130** — a photo that fails *after* the render says so, instead of spinning — paths: `app/routers/photos.py`, `tests/api/test_photo.py` — deps: none — DoD: `process_photo` commits `PROCESSING` before its `try` and then runs four more steps **outside** it — `_assign_renditions`, `images.file_digest`, `record_asset`, `db.commit()` — so a raise in any of them leaves the row in-flight for ever; `submit_with_session` does catch and log it, but its `db.rollback()` cannot undo a commit that already happened, and the owner is offered a retry on `FAILED` only, so what he sees is a tile that spins and a grid that never stops polling (F27's poll ends only when nothing is in flight); the fix is scope, not logic — the render-and-record sequence moves inside the guard that already exists, so a raise ends at `_fail()` with a translated reason exactly as a missing original does today; **two live windows are named and each gets a test**: `_assign_renditions` raising on an empty description — T125 made that raise *named* rather than an `IndexError`, it did not make it impossible — and `images.file_digest` raising `FileNotFoundError` when the original goes between `generate_derivatives` and the digest, which is the same `images.release` / `--prune` race T125 closed one door on; each test patches one step to raise and asserts the photo settles at `FAILED` with a reason and the grid stops polling, watched failing first; the happy path and `record_asset`'s F42 guarantee are unchanged, and **`recover_stuck_photos` is not touched** — it is the backstop that bounds this to "until the next restart", not the fix, and on a site that runs unattended for weeks that bound is the problem.
- [ ] **T129** — something outside the host watches `/healthz` — paths: `docs/HANDOFF.md` §7 — deps: none — DoD: the third part of R-03, per ADR-025; §7 gains the external check — what it polls, from where, how often, and what it does when it fails — set up by the owner and **verified by stopping `web` and watching the check go red**, not by watching it go green; `/healthz` itself is not modified; the section states plainly that no 500 or failed-photo notifier exists, points at ADR-025 for why, and says where to look instead — the log file T126 puts on disk — so the next reader does not go hunting for a notification that was never built.

## M16 — The editing mode, and a place of the owner's own *(serial, parent)*

> Iteration I4, from an owner request rather than an audit: the editing mode "works but is a little
> ugly" — «Выйти» sits in the middle of the screen covering the page, and «Показать правки» feels
> redundant while still being necessary. Intake, impact map and exit criteria:
> `docs/iterations/I4-editing-mode.md`. **Nothing here is visible to a visitor**, and that is an
> exit criterion, not an expectation: F36 and `test_anonymous_html_contains_no_admin_markup` stay
> the guard throughout.
>
> **T131 is a shared-primitive change and lands first, alone** — it rewrites `base.html` and the one
> component on every page, and both T132 and T133 hang off the menu it creates. T132 and T133 are
> independent of each other once T131 is in. T134 is isolated and may land at any point.
>
> **Three of these tasks change tests that are correct today.** Each such change is a behaviour
> change and carries its ADR: ADR-027 (the marker list), ADR-028 (the reveal model). Deferrals:
> ADR-031 (no version line), and the password stays in the environment (ADR-029).

- [x] **T131** — the owner's controls stop covering the page — paths: `app/templates/partials/nav.html`, `app/templates/base.html`, `app/templates/partials/admin_bar.html` (**deleted**), `app/static/css/admin.css`, `app/static/css/components.css`, `app/static/js/nav.js`, `app/i18n/ru/auth.json`, `tests/api/test_authz_sweep.py`, `e2e/` — deps: none — DoD: F61 met, per ADR-027; `admin_bar.html` is **deleted, not hidden**, and the navigation capsule gains one owner control — a dot indicator plus a button — opening a menu that holds the mode switch (T132 replaces its label and behaviour; until then it is today's «Показать правки», moved and working), «Кабинет» (added by T133 — **absent until then, never a dead link**) and the «Выйти» form with its CSRF field; **both clearance rules go** — `:root:has(body.is-admin)`'s `scroll-padding-block-end` and `body.is-admin .page`'s `padding-block-end` (`admin.css:28-34`) are deleted and nothing replaces them, proved by an e2e check that the same content renders to the same document height signed-in as anonymous, watched failing first against today's tree; the menu opens on click, closes on `Escape` and on a click outside, and **returns the caret to the button that opened it** — F-002's contract, and the caret must not land on `<body>`; `aria-expanded` is read back off the DOM rather than a variable, the mistake F-019 was; every control in the menu keeps the 24 px target from ADR-010 and a visible focus indicator, so `test_every_admin_focus_stop_shows_a_visible_indicator` passes unchanged; the marker list in `test_authz_sweep.py:118-122` **gains** the owner menu's class and **keeps `admin-bar`** — the list only ever grows, because a marker that can no longer appear costs nothing and a shortened list is a weakened guarantee; `e2e/test_nav_dropdown.py` still passes, and the mobile capsule below 768 px is checked at 360 px for the menu overhanging its containing block the way F-011 described.
- [x] **T132** — «Просмотр» and «Правка», instead of hover and a toggle on top of it — paths: `app/static/css/admin.css`, `app/static/css/components.css`, `app/static/css/photo.css`, `app/static/js/edits.js`, `app/templates/base.html`, `app/i18n/ru/auth.json`, `e2e/test_show_edits.py`, `e2e/test_a11y.py`, `e2e/test_admin_keyboard.py` — deps: T131 — DoD: F55 **as reformulated by I4**, per ADR-028; the three affordance families — `.editable__edit` (`admin.css:167-235`), `.site-links__edit` (`components.css:748-788`), `.photo-item__admin` (`photo.css:451-529`) — lose their `opacity: 0` resting state, their `:hover` / `:focus-visible` reveal, their `@media (hover: none)` branch and their `:root.show-edits` override, and one class on the root decides all three; in **«Просмотр»** the controls are **absent from the accessibility tree, not merely transparent** — asserted with `to_have_count(0)` or its equivalent, not with a computed `opacity`, which is the whole point of the change; in **«Правка»** every one of them is visible with the pointer nowhere near the page; the choice keeps living in `localStorage` under its existing key and keeps being applied by the pre-paint script in `base.html`, so a reload shows no flash of the wrong state; the switch reports its state from the DOM on every path — click, initial pass, an htmx swap that brings a new menu — and never from a variable; `e2e/test_show_edits.py` is **rewritten to the new model**, keeping its fourth test unchanged in intent (a visitor gets neither the control nor `edits.js`), with at least one check watched failing against today's tree first; `REVEAL_ADMIN_AFFORDANCES` (`test_a11y.py:111-122`) is replaced by entering «Правка» — the sweeps then measure the real state instead of simulating it through inline styles — and **the guard that made it fail when it revealed nothing is preserved in the new form**, because a sweep that silently measures the public page is worse than no sweep; `test_admin_keyboard.py`'s `_press_and_settle` enters «Правка» before it focuses anything and its docstring stops describing a resting state that no longer exists, while the focus-restoration behaviour it actually tests is untouched; contrast in both themes for the new outlined state, and `forced-colors` handled the way `test_forced_colors.py` requires.
- [x] **T133** — the cabinet: one page that says what needs the owner — paths: `app/routers/me.py` (new), `app/main.py`, `app/templates/pages/me.html` (+ partials as needed), `app/i18n/ru/me.json` (new area), `app/routers/seo.py`, `app/templates/partials/nav.html`, `tests/api/test_me.py` (new), `e2e/conftest.py`, `e2e/test_me.py` (new) — deps: T131 — DoD: F62 met, per ADR-029; one `GET /me` taking `OptionalAdmin` and **raising 404 when there is no session** — the same treatment a draft article gets, so the address does not confirm its own existence — with its own test asserting exactly `404` anonymous and `200` signed-in; **`test_authz_sweep.py:98-105` is deliberately not extended** (its cases assert redirect-to-login semantics, which this route does not have) and **nothing is added to any allow-list**; the page lists, each linking to the page that edits it: drafts (`Post.status == DRAFT`), unpublished albums, unpublished projects, every photograph at `status == FAILED` **with the retry control that `_photo_tile.html` already posts to**, and every photograph with no `alt`; every query reuses the shape the routers already use — no new model, no migration, and `selectinload` where a card needs its cover so the page does not fan out into N+1; when nothing needs attention the page says so through `partials/empty_state.html` rather than rendering blank; `noindex` in the head and `Disallow: /me` in `robots.txt`, with `sitemap.xml` unchanged; the «Кабинет» item appears in T131's menu; strings live in a **new** `app/i18n/ru/me.json` and no other area's catalogue is touched (CONVENTIONS); the page is added to `admin_surfaces` (`e2e/conftest.py:224-239`) so both accessibility sweeps cover it in both themes, and an e2e check creates a draft, an unpublished album and a failed photograph and finds all three on one screen, watched failing first; **the password is not on this page** — if anything about credentials is shown it is a sentence saying they live in the environment (ADR-029), and no form posts them.
- [x] **T134** — a code formatter stops rewriting prose — paths: `pyproject.toml` — deps: none — DoD: ADR-030; `docs` joins `extend-exclude` in `[tool.ruff]`; `uv run ruff format --check .` exits **0** over the whole tree and `uv run ruff check .` stays clean; `docs/iterations/I3-operations.md` is **byte-identical** afterwards — `git diff --stat` names no file under `docs/` from this task — so the T125 dedup excerpt keeps the aligned trailing comments that carry its explanation; `scripts/` is **not** excluded and is still both linted and formatted, verified by a deliberate mis-format there being caught before the task is ticked; the lint gate `publish.yml` runs is the same two commands and needs no change.

## M17 — Authoring: the visitor's view, video, and a cabinet with rooms *(serial, parent)*

> Iteration I5, from an owner request. Intake, impact map and exit criteria:
> `docs/iterations/I5-authoring.md`. Four of the seven parts were one complaint from four
> directions — **the mode I4 built covers three selector families and the rest of the owner's page
> ignores it** — which makes T135 a defect against F55 as already written, not a new requirement.
>
> **T135 is a shared-primitive change and lands first, alone**: it introduces the marker every
> other owner-only block is written against and edits twenty templates. T136 is independent and may
> land at any point. T138 lands before T139, because T139 documents what T138 builds. T137 is
> independent of all of them.
>
> **Three of these tasks change tests that are correct today**, each with its ADR: ADR-032 (the
> marker list and every flow that clicks an owner control), ADR-036 (the undescribed list leaves the
> cabinet). Deferrals: ADR-033 (search hits in «Просмотр»), ADR-034 (`pg_trgm`), ADR-035 (self-hosted
> video), ADR-037 (no pruning from the cabinet).
>
> **Expect the impact map to undercount the e2e tests, as I4's did twice** — T131 owed four more
> than it named, T132 owed seven. T135 widens the hidden set by about a factor of seven.

- [x] **T135** — «Просмотр» is the page a visitor reads, on every page — paths: `app/static/css/admin.css`, `app/static/css/components.css`, `app/static/css/photo.css`, `app/static/css/blog.css`, `app/static/css/dev.css`, `app/templates/` (the `is_admin` blocks in `base.html`, `partials/nav.html`, `partials/site_links.html`, `partials/editable.html`, `partials/empty_state.html`, `blog/index.html`, `blog/post.html`, `blog/_new_button.html`, `blog/_post_card.html`, `photo/index.html`, `photo/album.html`, `photo/_board.html`, `photo/_album_card.html`, `photo/_album_head.html`, `photo/_grid.html`, `photo/_photo_tile.html`, `photo/_uploader.html`, `dev/index.html`, `dev/_board.html`, `dev/_project_card.html`), `tests/api/test_authz_sweep.py`, `e2e/` — deps: none — DoD: **F55 met as written**, per ADR-032; one rule in `admin.css` — `:root:not(.show-edits) .owner-only { display: none }` — and every owner-only block carries the class, including the upload zone, «Новый альбом», the album action row, both reorder hints, the undescribed count line, `photo-item--undescribed`, the draft chips on album, project and post, the drafts section on `/blog`, unpublished album and project cards, and unfinished (`not ready`) photo tiles; **the three families I4 gated fold into the same marker** — `.editable__edit`, `.site-links__edit` and `.photo-item__admin` lose their `display: none` default and their `:root.show-edits` override, so exactly one mechanism decides the mode and `git grep -c 'show-edits' app/static/css` falls to the two rules that remain (the marker rule and the `.editable` outline); **two things stay visible in both modes and it is deliberate** — the mode switch itself, and the single line on a draft's or an unpublished album's own page saying a visitor would not see it, so the mode is faithful rather than misleading; `owner-only` **joins** the marker list in `test_authz_sweep.py:118-122` and nothing is removed from it; the proof is an e2e check that counts **rendered boxes** (`getClientRects().length`), never `opacity` and never DOM nodes, over `/`, `/blog`, `/photo`, `/photo/<album>` and `/dev`, comparing the signed-in owner in «Просмотр» against an anonymous visitor on the same content and finding the same set — watched failing first against today's tree, where the album alone differs by the uploader, the action row, two hints and every dashed rim; before starting, `e2e/` is grepped for locators naming `.upload-zone`, `photo-actions`, `status-chip`, `album-card__admin`, `project__admin`, `article__admin`, `drafts` and `_new_button`, and every flow that reaches one enters «Правка» through the existing `switch_mode` helper rather than being relaxed; `forced-colors` and both themes unaffected, since nothing gains a colour.
- [x] **T136** — search finds a word from its beginning — paths: `app/services/search.py`, `tests/api/test_search_seo.py` — deps: none — DoD: F65 met, per ADR-034; a tokeniser turns the normalised query into `token:* & token:*`, stripping `&|!()<>:*'` and dropping what is left empty, and the statement matches `websearch_to_tsquery('russian', q) || to_tsquery('russian', prefix)` with `ts_rank` over the same combined query, so **the change is a union and nothing found today stops being found**; when no usable token survives, the prefix half is omitted and the statement is byte-for-byte today's; `_statement` is written once for the hits and the count, as it is now, so `total` and the list cannot disagree; unit tests cover the empty query, a punctuation-only query, a query at `MAX_QUERY_LENGTH`, a token that is only metacharacters, and the case the owner asked for — «фотогр» finding «фотография» — watched failing first; **every existing assertion in `tests/api/test_search_seo.py` and `e2e/test_search.py` passes unchanged**, and a changed one is a finding, not a fix; no migration, no extension, no new index, and `MIN_QUERY_LENGTH` is not lowered.
- [x] **T137** — the cabinet gets rooms, and the undescribed list leaves it — paths: `app/routers/me.py`, `app/templates/pages/me.html` (+ `me_stats.html`, `me_media.html`, `partials/cabinet_nav.html`), `app/templates/partials/cabinet_group.html`, `app/static/css/me.css`, `app/i18n/ru/me.json`, `app/routers/seo.py`, `scripts/media_orphans.py`, `app/services/` (one new module), `tests/api/test_me.py`, `e2e/conftest.py`, `e2e/test_me.py` — deps: none — DoD: F64 met and F62 amended, per ADR-036 and ADR-037; three routes — `/me`, `/me/stats`, `/me/media` — each taking `OptionalAdmin` and **raising 404 without a session**, each `noindex, nofollow`, each with its own test asserting exactly 404 anonymous and 200 signed-in, and `seo.py`'s `Disallow: /me` verified to cover the prefix rather than assumed to; a shared `partials/cabinet_nav.html` names the rooms and marks the current one with `aria-current="page"`, and it is a real navigation landmark, not a row of buttons; **«Снимки без описания» is deleted from the cabinet** and the album's own prompt is untouched — `photo-item--undescribed` and the count line still render in «Правка» after T135; «Сводка» shows counts of published and draft articles, albums, projects, photographs by status, `SUM(media_asset.byte_size)` and the date of the last publication, every one a query over what is already there — **no new model, no migration, no new column**; «Медиа» renders photographs in flight and failed from the database on load, and the disk walk runs **only** on a press of «Проверить», answering into its own region via htmx, with the walk moved into a service that `scripts/media_orphans.py` then calls — the script's command-line output is diffed before and after and is identical, and **nothing on any page deletes a file**; all three rooms join `admin_surfaces` (`e2e/conftest.py:224-239`) so both accessibility sweeps cover them in both themes; strings stay in `app/i18n/ru/me.json` and no other catalogue is touched; the password sentence and ADR-029's position are unchanged.
- [ ] **T138** — a video can be part of an article — paths: `app/services/markdown.py`, `app/main.py`, `app/static/css/prose.css`, `app/static/js/video.js` (new), `app/templates/blog/post.html`, `app/templates/dev/detail.html`, `app/i18n/ru/`, `tests/unit/test_markdown.py`, `tests/api/test_pages.py` — deps: none — DoD: F63 met, per ADR-035; a paragraph holding nothing but a link to YouTube (`youtube.com/watch?v=`, `youtu.be/`, `/shorts/`), RuTube (`rutube.ru/video/<id>/`) or VK Video (`vk.com/video<oid>_<id>`, `vkvideo.ru/...`) renders as `<figure class="prose-video">` with a `<button>` carrying the embed URL in a data attribute — recognised by anchored per-host patterns, **never by a general URL parse**, so the value the sanitiser cannot filter is closed by the renderer exactly as `WIDTH_WORDS` is; a link of that shape wrapping an image from `/media/` uses that image as the poster, and a bare link gets a framed play control instead; a link to any other host, and a video link **among other text**, both stay ordinary links — the second is the same rule `_figure_paragraphs` already applies to pictures; `ALLOWED_TAGS` gains `button` and **`iframe` is not added**, raw HTML stays disabled at the parser, and a test asserts that a hand-written `<iframe>` in the source still does not survive; `app/main.py` gains `frame-src` naming the three hosts explicitly and **no other directive moves** — no `unsafe-inline`, no wildcard, `img-src` untouched — with the CSP assertion in `tests/api/test_pages.py` extended rather than loosened; `video.js` builds the `<iframe>` on press with `title`, `allowfullscreen` and the caret left on a control (F-002), is loaded only on pages that render prose, and a published page containing a video **carries no `iframe` in its HTML and makes no request to the video host until the press** — asserted in e2e by watching requests, watched failing first; the editor's preview goes through the same `render_markdown`, so preview and published page cannot diverge (F28); contrast and `forced-colors` for the new play control in both themes.
- [ ] **T139** — the editor says how the markup works — paths: `app/templates/blog/editor.html`, `app/static/css/blog.css`, `app/static/js/editor.js`, `app/i18n/ru/blog.json` — deps: T138 — DoD: F38's acceptance met in the form I5 gives it; the existing four-line size disclosure becomes one cheat sheet covering **picture width (`{.wide}`, `{.full}` and the default), caption, video, table, quote, code, link, list, heading and strikethrough**, each as a copyable snippet next to one sentence saying what it does — the width entries keep the wording that is there rather than being rewritten; the toolbar gains a **video** button and a **table** button, each inserting a working skeleton at the caret with the same placeholder mechanism the eight existing buttons use (`data-ph-*`), each with a `title` and a `visually-hidden` name from the catalogue, each meeting ADR-010's 24 px; the disclosure keeps `<details>`, stays **closed by default**, and its state is not persisted — it costs nothing once read; every string is a new key in `app/i18n/ru/blog.json` and **no key is renamed**, so nothing else that reads the catalogue moves; `e2e/test_a11y.py`'s editor sweep passes with the sheet both open and closed, and an e2e check presses the video button and finds the inserted snippet in the textarea, watched failing first.

## Closed milestones

Full text in `docs/tasks-archive.md`.

- **M0** — Scaffold
- **M1** — Design system + public shell
- **M2** — Auth, schema, admin core
- **M3** — Photography
- **M4** — Blog
- **M5** — Development
- **M6** — Search + SEO
- **M7** — Harden
- **M8** — Polish
- **M9** — Media lifecycle, quality and identity
- **M10** — UI audit, Phase A
- **M11** — UI audit, Phase B
- **M12** — Bounded indexes and honest search
- **M13** — HEIC on input
- **M14** — What the review found

